---

- hosts: "dev-ttt"
  become: yes
  become_method: sudo
  roles:
    - { role: mariadb, tags: mariadb }
    - { role: traintickets.to, tags: traintickets.to }

- hosts: "live-ttt"
  become: yes
  become_method: sudo
  roles:
    - { role: mariadb, tags: mariadb }
    - { role: traintickets.to, tags: traintickets.to }
